package com.dqhieuse.sportbookingbackend.modules.venue.entity;

public enum VenueStatus {
    PENDING,    // Chờ duyệt
    ACTIVE,     // Đang hoạt động
    REJECTED,   // Bị từ chối
    CLOSED,     // Chủ động đóng cửa (tạm thời)
}
